apiVersion: skaffold/v2beta9
kind: Config
build:
  artifacts:
    - image: netologygroup/dispatcher-migration
      docker:
        dockerfile: docker/migration.dockerfile
    - image: netologygroup/dispatcher
      docker:
        dockerfile: docker/Dockerfile
  tagPolicy:
    gitCommit: {}
  local:
    push: true
    useDockerCLI: true
    tryImportMissing: true
deploy:
  kustomize:
    paths:
    - deploy/k8s/overlays/ns
