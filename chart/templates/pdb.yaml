apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: {{ include "dispatcher.name" . }}-pdb
  labels:
    {{- include "dispatcher.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "dispatcher.selectorLabels" . | nindent 6 }}
